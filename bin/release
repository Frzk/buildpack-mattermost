#!/usr/bin/env bash

cat << EOF
---
addons:
 - scalingo-postgresql
config_vars:
  PATH: "/app/bin:/usr/local/bin:/usr/bin:/bin"
  MM_SERVICESETTINGS_LISTENADDRESS: ":${PORT}"
default_process_types:
  web: /app/bin/run
  postdeploy: /app/bin/postdeploy
EOF
